import "./App.css";

function App() {
  return (
    <div>
      <div>empty project and first change-div tag on master</div>
      <div>
        this tag added after building a new branch from master named develop
      </div>
      <div>
        After first commit on develop"above tag" i want to apply develop changes
        on master so what is the solution?? first i should commit this
        change'exactly this sentence' on develop and then switch to master and
        apply develop changes on master
      </div>
    </div>
  );
}

export default App;
